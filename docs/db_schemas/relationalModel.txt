competition={comp_id, Name, Description, lastRegistrationDate, lowestGrade, prefBranch, #user_id}

user = {user_id, email , class, (Password), branch, isTeacher,(shortName), }

interests = {int_id, #user_id, #comp_id, sign_up_date}

teacher={#user_id,shortName}

student={#user_id,branch,class}